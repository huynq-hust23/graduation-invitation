# Copy to .env.local and fill in your real details — .env.local is gitignored
# and never committed. Without it the site falls back to bracketed placeholders.

NEXT_PUBLIC_GRADUATE_NAME="[YOUR_NAME]"
NEXT_PUBLIC_PHONE_NUMBER=
NEXT_PUBLIC_EMAIL_ADDRESS=
